# Migration Review Ledger

**Status:** Generated review artifact; current classification summary refreshed 2026-08-08. It remains an input/provenance document, not the curated master or public Pages output.
**Generator:** `generate_migration_ledger.py`
**Ledger:** `migration_review_ledger.csv`

## Purpose

The ledger is the approval surface for migrating the existing spreadsheet into the proposed research-master schema. It retains every raw data row with its original CSV row number and values, alongside conservative proposed classification and metadata. It must be reviewed before any curated dataset is generated.

## Current classification summary

| Proposed disposition | Rows | Treatment |
|---|---:|---|
| `item` | 299 | Candidate flat catalogue records; IDs/codes are assigned only downstream in the curated master. |
| `blank_separator` | 31 | Empty presentation rows; retained as raw provenance only. |
| `series_context` | 21 | Annual/category labels used only to propose a `series` tag for following item candidates. |
| `research_note` | 10 | Editorial gaps, missing-material reminders, or ambiguous note rows; not items until identity is confirmed. |
| `source_context` | 5 | Landing/reference URLs embedded as title rows; not items. |
| `duplicate` | 8 | Duplicates of other item rows (Path to Spiritual Advancement truncated variant; legacy 2012 Discussion rows 318/321 — same talks as promoted masters 312/313; legacy row 296 — same NC audio edition as master 329, owner rulings 2026-08-07; raw streaming rows 249/250/251 — collapsed into promoted DVD masters 311/310 by the 2026-08-08 D-01 ruling; raw row 371 — the owner's 'WHAT IS THIS ⚠️' placeholder for the same work as promoted master 361, reclassified from `needs_review` by the 2026-08-09 owner ruling); retained as provenance, excluded from master. |
| **Total** | **374** | Matches the current source data rows and published JSON row count. |

> **Note:** the ledger was bootstrap-generated by `generate_migration_ledger.py`
> and is afterwards **hand-maintained** (dispositions evolve as rows are
> excluded, reclassified, or promoted), so this summary reflects the committed
> `migration_review_ledger.csv` as of 2026-08-09, not the bootstrap output.
> A `tests/test_pipeline.py` documentation-currency test fails if this table
> drifts from the ledger again.

## Safe proposal rules applied

- The raw source CSV is read only; it is not altered.
- The ledger itself does not assign IDs or catalogue codes; those are generated downstream in the curated master.
- `LSYYYYMM_part` values only suggest year/month, lecture type, DVD detail, and series context; they are retained unchanged as `raw_tempid`.
- ✅ and ❌ are only proposed as `true` and `false` on candidate item rows; blank stays blank.
- Existing valid Veritas product links are proposed as `source_url_veritas` on item candidates.
- **Resolved 2026-08-08:** the duplicated-prefix Advaita URL on raw rows 28–30 was corrected in the raw CSV and mirrored here; the current ledger no longer quarantines those three source links.
- The field `format` is not populated from the raw `format` column because that column contains a Discord URL rather than media-format data.
- No title is automatically canonicalized. The current title is copied only as a review proposal.

## How to review the CSV

Open `migration_review_ledger.csv` in GitHub, a spreadsheet app, or a CSV-aware editor. Review in this order:

1. Filter `disposition = item`; verify the title, series, type, year/month, format, ownership, and Veritas URL proposals.
2. Filter `disposition = needs_review` and `research_note`; decide whether each row is a confirmed missing item, source context, note, or should be excluded.
3. Verify the 2026-08-08 Advaita URL correction on raw rows 28–30; do not reintroduce the duplicated-prefix URL.
4. Review `series_context` rows before accepting series assignments for associated items.
5. Approve batches by collection (for example, lecture series first), rather than applying all rows at once.

## Known limitations

This is deliberately a conservative structural classification, not a claim that all 302 current `item` rows are complete research records. In particular, non-lecture material and post-ledger candidate/edition promotions are governed by their own reviewed inputs; the generated master/Pages outputs are the current publication state.
