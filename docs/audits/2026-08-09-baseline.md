# Baseline Audit — 2026-08-09

**Repo:** docsheet (56eli/docsheet)
**Auditor:** Arena.ai Agent Mode (expert full-stack + data engineering pass)
**Date:** 2026-08-09
**Commit audited:** `556bf48` (main HEAD = PR #43 merge) plus this session's
fix commits `f2d05c8`, `83b5c37`, `1886b84` on `arena/019fe659-docsheet`.
**Purpose:** baseline evidence for the persistent scoreboard
(`.scoreboard/scoreboard.yml`, `SCOREBOARD.md`).

## Scope audited

- Raw source spreadsheet → ledger → curated master → Pages JSON → static
  frontend (all layers).
- Repository setup: CI workflows, dependency pins, tests, docs, git hygiene.
- ~20 independent pandas probes bypassing the project's own validators.

## Commands / checks run

| Check | Result |
|---|---|
| `process_data.py --check` | ✅ docs/data.json current (374 rows) |
| `build_research_master.py --check` | ✅ 362 items; 75 excluded; 134 overrides; 39 candidates |
| `build_catalogue_pages.py --check` | ✅ 362 Everything rows |
| `reconcile_research_master.py --check` | ✅ report current |
| `map_series_taxonomy.py --check` | ✅ 186 mappings; 0 queued |
| `sync_inventory_mirrors.py --check` | ✅ mirrors match |
| `python -m unittest discover tests` | ✅ Ran 126, OK |
| `coverage run -m unittest discover tests && coverage report` | ✅ 91% (floor 85) |
| `node --check docs/app.js` + all `tests/*.spec.js` | ✅ clean |
| `npm ci` | ✅ (Playwright browser download ❌ — CDN blocked in sandbox) |
| Static serve of `docs/` (all assets) | ✅ 200 |
| `gh run list` / `gh issue list` | ✅ CI + Pages green; 1 open issue (#18) |

## Files reviewed

README.md, INSTRUCTIONS.md, NEXT_AGENT_HANDOFF.md,
FULL_STACK_AUDIT_2026-08-09_ARENA_EXPERT.md (and prior audits),
process_data.py, build_research_master.py, build_catalogue_pages.py,
reconcile_research_master.py, map_series_taxonomy.py,
sync_inventory_mirrors.py, fetch_veritas_catalogue.py, _common.py,
tests/test_pipeline.py, tests/*.spec.js, .coveragerc, requirements*.txt,
package.json/package-lock.json, .gitignore, .github/workflows/*.yml,
migration_review_ledger.csv, all data/*.csv, all docs/*.json, docs/app.js,
docs/index.html, docs/style.css, decisions/, archive/README.md.

## Findings by aspect (AI scores)

All 22 aspects were scored; details (evidence, next actions, files) live in
`.scoreboard/scoreboard.yml`. Summary:

| Aspect | AI | Confidence | Status |
|---|---:|---|---|
| project_purpose_scope | 9 | high | healthy |
| readme_onboarding | 9 | high | healthy |
| repo_organization | 7 | high | needs_work |
| code_hygiene | 7 | high | needs_work |
| architecture | 9 | high | healthy |
| maintainability | 7 | high | needs_work |
| type_safety_validation | 8 | high | healthy |
| error_handling_logging | 8 | high | healthy |
| dependency_hygiene | 9 | high | healthy |
| tests | 9 | high | healthy |
| ci_cd | 9 | high | healthy |
| security_privacy | 8 | high | healthy |
| performance | 8 | medium | healthy |
| github_pages_presentation | 9 | medium | healthy |
| ux_usability | 9 | high | healthy |
| accessibility | 8 | medium | healthy |
| content_quality | 9 | high | healthy |
| feature_completeness | 8 | high | healthy |
| deployment_readiness | 9 | high | healthy |
| agent_readiness | 9 | high | healthy |
| task_hygiene | 8 | high | healthy |
| auditability | 9 | high | healthy |

Sub-target aspects (7s) are the only `needs_work` items: repo_organization
(18 root .md files), code_hygiene and maintainability (large generator
monoliths, refactor in progress).

## Risk flags

- **CSP `style-src 'unsafe-inline'`** (low severity) — required for dark-mode
  toggles; script-src hash-pinned, Tabulator SRI-pinned. Not accepted by the
  owner yet; visible in SCOREBOARD.md and scoreboard.yml.

## Manual workflow edits needed

None. `.scoreboard/manual-workflow-edits.md` documents the policy; the three
existing workflows (ci, update_spreadsheet, map_veritas_catalogue) are
current and green.

## Quality gate result

`repo_ready` = **warning**

- Overall effective score: **8.4** (weighted average, 22 aspects, 701/83).
- Required thresholds: security_privacy 8 ✅, tests 9 ✅, readme_onboarding 9 ✅,
  ci_cd 9 ✅, agent_readiness 9 ✅.
- `warning` (not `pass`) because an active low-severity risk flag exists and
  several scores are medium-confidence (browser suite CI-verified only; no
  automated a11y scan; no browser-side performance benchmark).

## Recommended next actions

1. Split the large generator modules (`code_hygiene` / `maintainability`).
2. Archive/consolidate superseded root audits (`repo_organization`).
3. Triage issue #18 (owned-flags vs. lak.nz Drive; needs owner access).
4. Optional: axe-core accessibility scan; Lighthouse pass on the Pages site.
5. Owner may provide `user_score` values for any aspect to steer priority.

## Limitations of this audit

- Playwright browsers cannot be downloaded in the sandbox (CDN TLS reset), so
  the 19 browser specs were not re-run locally; CI is green and the specs
  were reviewed statically.
- The live site `https://56eli.github.io/docsheet` is unreachable from the
  sandbox network; Pages deployment status was verified via `gh run list`.
- No automated accessibility scan and no browser-side performance benchmark
  were run.
- No destructive commands or new repo dependencies were introduced.

## Files intentionally not changed

- `.github/workflows/*` — policy: never edited without explicit user
  instruction; none needed currently.
- Master/ledger data CSVs and `docs/*.json` — this audit was read-only for
  data (fixes from the audit were committed separately: ledger row 371
  reclassification, docs corrections, frontend toggle).
